pipeline:
  docker:
    image: plugins/docker
    repo: phaefelfinger/tv7playlist
    tags:
      - latest
      - 2.0
      - 2.0.1
    secrets: [ docker_username, docker_password ]
    debug: true
    when:
      event: push
      branch: master